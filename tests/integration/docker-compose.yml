services:
  test-cli:
    build:
      context: ../..
      dockerfile: tests/integration/Dockerfile
    container_name: mcp-test-suite
    volumes:
      - test-home:/home/testuser
    environment:
      - HOME=/home/testuser
      - NODE_ENV=test
    working_dir: /app

volumes:
  test-home:
